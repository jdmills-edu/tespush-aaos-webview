<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- Local network access permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    
    <!-- Widevine DRM support -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- Chromecast receiver permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.NFC" />
    
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="true" />
    
    <!-- Hardware acceleration support -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="false" />
    
    <!-- Widevine DRM feature -->
    <uses-feature
        android:name="android.software.webview"
        android:required="true" />
        
    <!-- Automotive distraction optimization -->
    <uses-feature
        android:name="android.software.car.templates_host"
        android:required="false" />
        
    <!-- Immersive experience support -->
    <uses-feature
        android:name="android.software.car.projection_host"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:appCategory="audio"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true">
        
        <!-- Android Automotive Optimizations -->
        <meta-data
            android:name="distractionOptimized"
            android:value="true" />
        <meta-data
            android:name="android.car.application"
            android:resource="@xml/automotive_app_desc" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.APP_BROWSER" />
            </intent-filter>
            
            <!-- Mark this activity as distraction optimized -->
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
                
            <!-- Immersive mode support -->
            <meta-data
                android:name="android.app.immersive"
                android:value="true" />
                
            <!-- Automotive specific metadata -->
            <meta-data
                android:name="android.app.automotive.ACTIVITY_TYPE"
                android:value="media" />
        </activity>
        
        <!-- Chromecast Receiver Service -->
        <service
            android:name=".cast.ChromecastReceiverService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="com.adamucf.simpleaacpbrowser.CAST_RECEIVER" />
            </intent-filter>
        </service>
        
        <!-- Cast Media Route Provider Service -->
        <service
            android:name="com.google.android.gms.cast.framework.media.MediaNotificationService"
            android:exported="false" />
            
        <!-- Cast Options Provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.adamucf.simpleaacpbrowser.cast.ChromecastReceiverService$CastOptionsProvider" />

    </application>


</manifest>